import styled from "styled-components";
import { theme } from "../../../styles/theme";

export const ChartContainer = styled.div`
  border: 1rem solid ${theme.palette.secondary._};
  border-radius: 1rem;
  background-color: ${theme.palette.white._};
`;
